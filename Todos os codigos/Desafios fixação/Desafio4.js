// 4

function descont(){
    let desconto = parseInt(prompt("Quantos produtos deseja comprar"));
    if ( desconto >= 10 ){
        alert ("Você possui desconto")
    }
    else {
        alert ("Você não possui desconto")
    }
}

descont ();

function vipp(){
    let vip = prompt("Você possui o vip")
    if (vip = "sim"){
        alert ("Você possui desconto")
    }
    else {
        alert ("Você não possui desconto")
    }
}
 vipp ();